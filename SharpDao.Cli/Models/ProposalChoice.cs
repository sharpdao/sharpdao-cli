using Schema.NET;

namespace SharpDao.Cli.Models;

public class ProposalChoice: Thing
{
    //name = the name/text of the choice
    //subjectOf = the Proposal
    //potentialAction = the Action that will be taken if this choice passes
}